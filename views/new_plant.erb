<h1>
    <i class="fas fa-leaf"></i>    Add a new <u><%= @plant[:plant_title] %></u>

</h1>

# need to change the questions and do the name = thing so that I can map it to the right table. unique_plants?

<form action="/plants/<%= @plant[:plant_id] %>/uniqueplant/create">
  <div class="form-group">
    <label>Are you going?</label>
    <select class="form-control" name="going">
      <option value="1">Yes</option>
      <option value="0">No</option>
    </select>
  </div>
  <div class="form-group">
    <label>Comments</label>
    <textarea class="form-control" rows="3" name="comments"></textarea>
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
</form>